### Git ###
# Создано git для резервного копирования. Чтобы отключить резервное копирование в Git:
# $ git config --global mergetool.keepBackup false
* .orig

# Создано git при использовании инструментов слияния для конфликтов
*.РЕЗЕРВНОЕ КОПИРОВАНИЕ.*
*.БАЗА.*
*.МЕСТНЫЙ.*
*.УДАЛЕННЫЙ.*
* _BACKUP _ *. Txt
* _BASE _ *. Txt
* _LOCAL _ *. Txt
* _REMOTE _ *. Txt

### Linux ###
* ~

# временные файлы, которые могут быть созданы, если у процесса еще есть дескриптор удаленного файла
.fuse_hidden *

# Настройки каталога KDE
.directory

# Корзина для мусора Linux, которая может появиться на любом разделе или диске
.Trash- *

# Файлы .nfs создаются, когда открытый файл удаляется, но к нему все еще обращаются
.nfs *

### Текст ###
* .doc
* .docx
*.журнал
* .msg
* .pages
* .rtf
*.текст
* .wpd
* .wps

# Конец https://www.toptal.com/developers/gitignore/api/linux,git,text
